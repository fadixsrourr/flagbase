{
  "name": "@flagbase/functions",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "serve": "firebase emulators:start --only functions",
    "clean": "rm -rf lib"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@flagbase/types": "workspace:*",
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.3.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
